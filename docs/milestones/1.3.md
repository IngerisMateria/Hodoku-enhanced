# Milestone 1.3 — Prompt verbatim

```
Leé CLAUDE.md y docs/plantilla-tecnica.md. Arrancamos el milestone 1.3:
bent subsets n=5..9, entradas separadas (decisión del dueño). Archivá este
prompt verbatim en docs/milestones/1.3.md (regla a).

BACKLOG DE PULIDO (mecanismo nuevo, pedido del dueño):
1. Creá docs/pulido.md con el contenido delimitado al final, verbatim.
   Sumá a las Reglas de trabajo de CLAUDE.md: "Objeción de diseño no
   bloqueante → entrada en docs/pulido.md con motivación completa; se
   procesan en el milestone 5.0". Agregá al roadmap, antes de 5.x:
   "- [ ] 5.0 Pasada de pulido: procesar todas las entradas de
   docs/pulido.md".

TÉCNICAS (el motor ya está; TYPE_BY_SIZE solo registra tamaños):
2. SolutionTypes nuevos con library codes de 4 dígitos libres y
   documentados: VWXYZ_WING (n=5, "VWXYZ-Wing"), UVWXYZ_WING (6),
   TUVWXYZ_WING (7), STUVWXYZ_WING (8), RSTUVWXYZ_WING (9). Registralos
   en el motor y en el registro de formateadores (un formateador bent
   genérico parametrizado, no cinco copias).
3. Options: entrada separada por tamaño con score custom escalonado
   (decisión del dueño: NO tab con límite global), índices consecutivos
   entre Bent Quad (5650) y ALS-XZ (5700), scores crecientes razonables
   dentro de la escala existente (documentá los elegidos y por qué).
   Defaults estilo fish/Leviathan: n=5 enabled=true; n=6..9
   enabled=false con allSteps=true (existen, desglosadas, apagadas).
4. Hints en intl/ para los cinco tipos.

FIXTURES Y CUSTODIA:
5. n=5: ≥3 positivos + ≥2 negativos cosechados de los corpus. n=6..9:
   cosechá lo que exista en corpus/te2/te3; para cada tamaño sin caso
   natural, construí 1 positivo a mano validado con el harness y
   documentá la rareza en el log.
6. Matiz de la regla de custodia (actualizá CLAUDE.md y la plantilla):
   la custodia por snapshots aplica a técnicas enabled por default; para
   las apagadas por default, la custodia son sus fixtures de librería.
   Aplicá: si algún puzzle de te2 tiene VWXYZ en el path default, sumalo
   al corpus y regenerá snapshots reportando el diff; si ninguno,
   documentalo y la custodia queda en fixtures.
7. Verificación GUI de 1 positivo VWXYZ (flujo de siempre) y contame.

MÉTRICAS para el log: pasos por tamaño (5..9) en all-steps sobre corpus,
te2-100 y te3-200; duración de esa pasada all-steps (sanity de
performance de los tamaños grandes); revalidación soundness completa de
lo cosechado.

Criterio de aceptación: suite verde local y en CI; fixtures de los cinco
tamaños corriendo; pulido.md creado con sus 2 entradas y la regla + 5.0
en CLAUDE.md; GUI verificada. Al cerrar: commits lógicos, push, link del
run, tick 1.3 + PRÓXIMO en 1.4, log técnico + resumen llano.

--- CONTENIDO PARA docs/pulido.md (verbatim) ---
# Backlog de pulido — objeciones de diseño diferidas

Se procesan en el milestone 5.0, cuando no quede nada más en el roadmap.
Cada entrada registra el porqué completo, para que el futuro no tenga que
reconstruirlo.

## P-001 — Presentación de subtipos del WXYZ-Wing canónico
- Fecha: 2026-07-21 · Origen: dueño · Estado: diferido (se queda como está)
- Contexto: el hint reporta "Type 1/2" (T1: Z en la bisagra; T2: Z ausente
  de la bisagra).
- Objeción: no hay consenso teórico sobre esta taxonomía; SudokuWiki usa
  dos tipos y en sus comentarios objetan que el Tipo 2 es (o se solapa
  con) un Sue de Coq. Al dueño le gusta la salida actual; el riesgo es
  cosmético: reclamos de puristas post-release.
- Cambio candidato al pulir: hacer configurable u omitible la etiqueta de
  subtipo, y/o documentar en el help nuestra taxonomía y su relación con
  SDC / ALS-XZ / bent sets (proyecciones de la misma álgebra de dos ALS
  con comunes restringidos).
- Nota técnica: el subtipo NO se almacena — se deriva (T1 sii la bisagra,
  primer índice del paso, aparece en fins). Cambiar la presentación es
  tocar solo el formateador en ModernStep: costo mínimo, cero migración.
- Criterio de cierre: decisión explícita del dueño (mantener / configurar
  / omitir) + help actualizado.

## P-002 — Separar Kraken Fish Type 1 y Type 2 en la configuración
- Fecha: 2026-07-21 · Origen: dueño (dolor real del HoDoKu base) ·
  Estado: diferido
- Contexto verificado en la base: una sola StepConfig KRAKEN_FISH
  (default off) gobierna todo kraken; los tipos 1/2 que se ven en los
  listados no tienen enable, score ni orden propios. Consecuencia:
  imposible habilitar T1 sin T2 en find-all-steps, en el solver y en la
  generación por técnica.
- Motivación del dueño (sus palabras): "hodoku siempre se cuelga
  analizando all possible steps porque hay cientos de kraken fish type 2
  y ninguno te sirve como deducción porque igual nunca los encontrarías;
  es una estrategia de computadora; en cambio los type 1 sí es posible".
- Cambio deseado: StepConfigs separadas para T1 y T2 (enable, score y
  orden propios), respetando el principio de desglose, visibles y
  filtrables en las tres superficies: find-all-steps, solver y
  training/generador.
- Alcance técnico estimado: SolutionType/Options/StepConfig, paneles de
  config, el camino kraken de FishSolver; confirmar al implementar cómo
  se representa hoy el subtipo (enum vs texto del paso); migración de
  hcfg viejos (una sola entrada). Snapshots no deberían cambiar (kraken
  default off).
- Nota de planificación: encaja naturalmente en el milestone de desglose
  de fish (hoy 1.14) si el dueño decide adelantarlo; por defecto, 5.0.
- Criterio de cierre: en la GUI se puede habilitar T1 sin T2 en las tres
  superficies, y un all-steps sobre un puzzle kraken-pesado termina en
  tiempo razonable con T2 apagado.
--- FIN DEL CONTENIDO ---
```
