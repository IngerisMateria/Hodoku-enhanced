# Milestone 1.4 — Prompt verbatim

```
Leé CLAUDE.md, docs/estrategia-taxonomia.md y docs/sudoku_mapa_relacional.md
(los dos últimos recién agregados por el dueño: commitealos). Arrancamos el
milestone 1.4: Registro de técnicas y opciones v1. Archivá este prompt
verbatim en docs/milestones/1.4.md (regla a).

CONTEXTO NUEVO (queda registrado al archivarse): el proyecto entra en modo
presupuesto-consciente. Decisiones del dueño: maximizar novedad teórica por
iteración hasta una release pública financiable; M/S/L/H y el desglose fino
de fish se difieren (vuelven con el motor de cadenas); carpetas/presets/
resaltado se difieren a backlog; kraken T1/T2 se pliega al 1.5.

ROADMAP Y REGLAS:
1. CLAUDE.md — reescribí el tramo pendiente del roadmap así:
   1.4 Registro de técnicas y opciones v1 + experimento T2
   1.5 Aside v2 + buscador en config + split Kraken T1/T2 (P-002)
   1.6 Oddagons I: Broken Wing + Bivalue Oddagon (framework guardianes)
   1.7 Oddagons II: Tridagon* (fixtures: subset te3)
   1.8 Uniqueness Pack: Unique Loop + Extended UR + BUG-Lite + Reverse BUG + MUG
   1.9 SDC extendido básico = régimen R0 del motor bent (locked n=4..9)
   1.10 3D Medusa + GEM (+ hallazgo B: coloring→generador)
   1.11 Fireworks*
   2.x Arco multi-sector*: SK-Loop, MSLS, SET
   3.x Arco Exocet*: JE → Double → JE+ → SE
   4.x Cadenas: motor AIC v2 (acá vuelven M/S/L/H, Nishio, dynamic/nested)
   5.0 Pulido (backlog) · 5.x recalibración, docs
2. Sumá a Reglas de trabajo: "toda opción de configuración tiene pestaña clásica canónica (GENERAL/SOLVER/ALL POSSIBLE STEPS/STEPS/COLORS, pensar en cuál a juzgar por lo que contiene cada una) y aparece además en el aside de sus técnicas dueñas".
3. pulido.md: P-002 → estado "plegada al milestone 1.5 (el des-colapso es
   acotado: el enum ya tiene TYPE_1/0371 y TYPE_2/0372; solo la config los
   colapsa)". Agregá P-003 — memoria de posición de popups (todos los
   popups recuerdan dónde los movió el usuario; global si es eficiente).
   Agregá P-004 — carpetas/presets/resaltado de solapamiento diferidos
   post-release por presupuesto: 3 vistas (por familia, por convención,
   jerárquica por las 4 raíces del mapa del dueño) + custom del usuario;
   las vistas son SOLO visuales (el orden vive únicamente en la lista
   plana exhaustiva) y una técnica puede repetirse en varias carpetas;
   presets de enable para find-all-steps; diseño completo en
   estrategia-taxonomia.md §5 y en este prompt.

REGISTRO (paquete nuevo, p.ej. solver.modern.registry — SIN UI en este
milestone; la UI es 1.5):
4. TechniqueInfo: id (SolutionType), displayNameDefault, aliases[],
   descripción corta (1-3 líneas), familia, motor/finder, subsumedBy[]
   (taxonomía multi-padre; semilla: el mapa del dueño), régimen si aplica
   (R1/R0), referencias opcionales. Dos relaciones DISTINTAS: taxonomía
   ("es caso de") y propiedad de configuración ("mi comportamiento
   depende de estas opciones"); no las mezcles — la herencia del aside
   (1.5) va por propiedad, las carpetas/alias van por taxonomía.
5. OptionInfo: clave en Options, descripción llana DERIVADA DE LEER EL
   CÓDIGO (nunca adivinada), dueños (≥1; caso testigo: allowDualsAndSiamese
   cubre Dual 2-String-Kite + Dual Skyscraper + Siamese Fish), y pestaña
   UI actual. Cobertura mínima: TODO ConfigStepPanel inventariado.
   En allowAlsOverlap aclará que NO es doubly-linked (eso se chequea
   siempre); documentá la semántica real de onlyOneAlsPerStep desde el
   código.
6. Cobertura: toda técnica con StepConfig (~96 legacy + 8 modernas) tiene
   fila, con aliases sembrados desde el mapa (SDC/foro incluidos).
7. API de display-name (default→alias preferido) consumida por los
   formateadores de ModernStep + estructura de persistencia de la
   preferencia en Options (sin UI). Hints legacy NO se tocan (incremento
   futuro, anotado).
8. Tests de custodia: (a) completitud — falla si hay SolutionType
   configurado sin fila (obliga a registrar toda técnica futura);
   (b) DAG de subsunción acíclico; (c) sin colisiones exactas de alias
   entre técnicas distintas.

EXPERIMENTO T2:
9. Sobre todos los canónicos T1/T2 cosechados: verificá por estado que
   (a) hay exactamente un candidato no restringido, (b) el AlsSolver
   produce un ALS-XZ simple cuyas eliminaciones cubren las del wing,
   (c) no hace falta estructura locked/doblemente-enlazada. Reporte con
   porcentajes y contraejemplos en docs/experimentos/t2-regimen.md;
   actualizá P-001 con los datos.

Criterio de aceptación: suite verde local y en CI con los tests del
registro; registro completo; experimento documentado; cero UI nueva;
legacy tocable solo la lista blanca vigente. Al cerrar: commits lógicos,
push, link del run, tick 1.4 + PRÓXIMO en 1.5, log técnico + resumen
llano.
```
