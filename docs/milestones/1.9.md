# Milestone 1.9 — Prompt original (archivado verbatim)

> Regla a) del CLAUDE.md: al arrancar cada milestone, su spec/prompt completo se archiva
> verbatim en `docs/milestones/NN.md`.

```
Leé CLAUDE.md, docs/estrategia-taxonomia.md (§3-5, el registro) y
docs/pulido.md (P-003, P-004). Arrancamos el milestone 1.9, REDEFINIDO por
el dueño: consolidación de UI y taxonomía. CERO teoría/álgebra nueva —
todo sobre estructura ya construida (registro del 1.4, aside/buscador del
1.5, framework de config). El SDC/R0 que estaba acá se difiere a la cola
de teoría (para Fable). Archivá este prompt verbatim en
docs/milestones/1.9.md (regla a).

ROADMAP: reescribí el tramo en CLAUDE.md — 1.9 pasa a "Consolidación UI y
taxonomía" (este prompt); el SDC/R0, 3D Medusa/GEM y Fireworks se corren;
sumá al final de la lista de teoría, marcadas [Fable]: "SDC extendido/R0",
"M/L/H/S/T-Wings (AICs de forma fija, requieren motor de cadenas v2)",
"ALS-W-Wing/Ext-SDC/AHS". El editor de colores (P-004→) queda como
milestone propio futuro (ver punto 6, NO se hace acá).

1. DESGLOSE DEL UNIQUENESS PACK (#1 del dueño — fundacional, va primero).
   Las 5 técnicas del pack hoy comparten UniquenessPackSolver con 5
   SolutionType pero NO tienen presencia configurable propia bien ubicada.
   - Extended UR: desglosá en EXTENDED_UR_TYPE_1 y _TYPE_2 (StepConfigs
     propios, como el UR legacy hace con sus tipos), library codes libres
     documentados, honrados por solver/all-steps/progress/training. El
     solver filtra por tipo pedido (patrón kraken 1.5). Migración one-shot
     de hcfg de la entrada única a las dos.
   - Las otras 4 (Unique Loop, BUG-Lite, Reverse BUG, MUG) quedan como
     entrada única cada una (su forma natural — no hay subtipos en el enum
     que separar; si aparecen a futuro se desglosan entonces, anotalo).
   - Registro: filas para los tipos nuevos (el test de completitud las
     exige solo); actualizá subsunciones.
   - Snapshots: verificá que el desglose no cambie ningún solve path
     (mismas deducciones, distinta etiqueta). Test.

2. CATEGORÍA/CARPETA "ODDAGONS" (#3) + infra de carpetas (#3 general).
   Decisión del dueño: carpetas SOLO VISUALES; el orden de resolución
   vive únicamente en la lista plana exhaustiva (P-004); una técnica puede
   repetirse en varias carpetas. Implementá el modelo de carpetas como
   vista sobre el registro (agrupación por familia), aplicado a las 4
   superficies: solver, all-possible-steps, progress, training.
   - Creá la carpeta "Oddagons": Broken Wing (hoy en Wings), Bivalue
     Oddagon y Tridagon (hoy en Miscellaneous) van ahí. Broken Wing puede
     además seguir apareciendo en Wings si el modelo multi-carpeta lo
     permite gratis; si no, priorizá Oddagons.
   - Carpeta "Uniqueness" (decisión del dueño: todo junto): UR legacy
     (todos sus tipos) + las 5 del pack (con ExtUR ya desglosado) en una
     sola carpeta Uniqueness.
   - El resto de familias del registro (Wings, Fish, ALS, Single-digit,
     Coloring, Chains, Basics…) pobladas desde la taxonomía existente.
     Alcance: agrupación visual funcionando; NO rediseñes la lista plana.

3. BUSCADOR EN PROGRESS Y TRAINING (#4). Extendé el mismo componente de
   búsqueda del 1.5 (nombre + aliases + descripción del registro) a las
   pestañas Progress y Training. Con filtro activo, bloqueá el
   reordenamiento donde el orden importe (mismo cuidado que en solver).

4. TOGGLE LISTA/CARPETA EN ALL-POSSIBLE-STEPS (#5). Hoy all-steps fuerza
   vista por carpetas y no tiene los botones de alternancia que otras
   pestañas sí. Agregá el toggle lista-plana / carpetas, consistente con
   el resto de la UI. Preferencia persistida.

5. MEMORIA DE POSICIÓN DE POPUPS (#2 / P-003). Que los diálogos recuerden
   la última posición donde el usuario los movió, en vez de reaparecer en
   la posición genérica. Global si es eficiente (un helper compartido al
   mostrar/cerrar, persistido en Options); si algún diálogo no encaja en
   el mecanismo común, lista las excepciones. Marcá P-003 como ejecutada
   en pulido.md.

6. NO EN ESTE MILESTONE — editor de colores por estrategia (#6 del dueño).
   Es un milestone propio (toca ConfigColorPanel + el modelo de color de
   cada técnica + persistencia + rendering). Registralo en pulido.md como
   P-005 con el diseño que discutió el dueño (colores de highlight
   editables por parte de cada estrategia; hoy todo es verde/rojo/algo de
   azul), para hacerlo cuando el dueño decida. NO lo implementes.

VERIFICACIÓN (capturas revisadas + contame):
(a) el solver mostrando la carpeta Oddagons con sus 3 técnicas y la
carpeta Uniqueness con legacy+pack; (b) Extended UR Type 1 y Type 2 como
entradas separadas; (c) el buscador funcionando en Progress y en Training;
(d) all-steps alternando entre lista y carpetas; (e) un popup reabriéndose
donde lo dejaste. Matriz de config UX del 1.8 (apply-on-tab-change,
refresh-on-enter) sigue verde tras estos cambios.

Criterio de aceptación: suite verde local y en CI (test del desglose +
completitud del registro exigiendo las filas nuevas + lo existente); las 5
verificaciones GUI; migración de hcfg testeada; P-003 ejecutada y P-005
registrada; cero cambios de solve paths; cero álgebra de solving tocada.
Al cerrar: commits lógicos (desglose / carpetas / buscador / toggle /
popups separados), push, link del run, tick 1.9 + PRÓXIMO en 1.10, log
técnico + resumen llano.
```
