(Es el prompt original del milestone 0.1, ya completado. NO ejecutar.)

Leé CLAUDE.md. Vamos con el milestone 0.1.

Contexto clave: el repo no trae build system, y los recursos (img/, intl/,
help/, templates.dat) viven adentro de src/ — tienen que terminar en el
classpath del jar. El punto de entrada es sudoku.Main (ojo: varios diálogos
Swing tienen mains de prueba propios).

Tareas:
1. Verificá si tengo un JDK 17 o 21 instalado; si no, guiame para instalarlo.
2. Armá un build Gradle mínimo que compile todo src/ y empaquete un jar
   ejecutable con los recursos incluidos.
3. Si hay errores o warnings bloqueantes con JDK moderno, corregilos con el
   cambio mínimo posible. Nada de refactors.
4. Documentá build y ejecución en docs/build.md.
5. Agregá un workflow de GitHub Actions que compile en cada push/PR.

Criterio de aceptación: clon limpio + un comando documentado produce el jar,
la GUI abre y carga un puzzle, y el workflow queda en verde. No toques
lógica de solving.
