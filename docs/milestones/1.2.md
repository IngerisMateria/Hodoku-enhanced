# Milestone 1.2 — Prompt verbatim

```
Leé CLAUDE.md y docs/plantilla-tecnica.md. Arrancamos el milestone 1.2 con
alcance REDEFINIDO por el dueño: desglose canónico/general del WXYZ +
motor paramétrico. La extensión n=5..9 pasa al milestone 1.3 (punto 10).
Archivá este prompt verbatim en docs/milestones/1.2.md (regla a).

PRINCIPIO NUEVO (sumalo a las Reglas de trabajo de CLAUDE.md):
Principio de desglose (estilo HoDoKu): toda generalización que subsuma
casos nombrados de la literatura expone esos casos como entradas separadas
del solver (SolutionType, score, enable y trainer propios), ordenadas
antes que la generalización. Precedente: Skyscraper / 2-String Kite /
Empty Rectangle coexisten con Turbot Fish.

RENOMBRE (lo shippeado en 1.1 es el general, no el canónico):
1. SolutionType.WXYZ_WING → BENT_QUAD, display "Bent Quad", conservando
   el library code 0802 (fixtures siguen andando sin tocar). Renombrá el
   fixture a test/fixtures/libs/bent-quad.txt. Snapshots: 0/30 paths lo
   contenían, no debería cambiar ninguno — verificalo. Strings de intl
   actualizados. Anotá en el log que el enum viejo muere (saves .hsol de
   estos días quedan huérfanos: aceptable pre-release).

REFACTOR (custodiado):
2. Generalizá el finder a un motor bent-subset con n paramétrico
   (p.ej. solver.modern.BentSubsetSolver); BENT_QUAD = caso n=4. Criterio
   duro: suite completa en verde SIN regenerar ningún snapshot.
3. Consolidá WxyzWingStep en UNA subclase genérica (ModernStep) con
   registro de formateadores de hint por SolutionType (cero campos
   nuevos, constructor público sin argumentos, solo toString).
4. Dedup del nit del 1.1: igualdad de paso (tipo + celdas +
   eliminaciones) en el motor.

TÉCNICA NUEVA — WXYZ-Wing canónico:
5. SolutionType.WXYZ_WING nuevo (display "WXYZ-Wing", library code de 4
   dígitos libre, documentado), finder propio en solver.modern.
   Definición: una bisagra H y tres alas w1,w2,w3 donde cada ala ve a H;
   la unión de candidatos de las 4 celdas es exactamente {W,X,Y,Z}; cada
   ala es bivalue {a,Z} con a distinto, cubriendo {W,X,Y}. Tipo 1: Z ∈ H
   (H tiene los 4 candidatos). Tipo 2: Z ∉ H (H = {W,X,Y}). Excluí el
   degenerado donde las 4 celdas se ven todas entre sí (naked quad).
   Eliminaciones: Z de toda celda externa que vea a TODOS los portadores
   de Z del patrón (las alas, más H si es tipo 1). Justificación para el
   hint: sin Z en el patrón, cada ala queda forzada a su letra, y H debe
   tomar una de esas tres letras chocando con el ala que la porta (la
   ve): contradicción. El hint reporta el tipo (1/2), estilo subtipos de
   fish. NOTA: la bisagra puede repartir alas en fila+columna+caja — hay
   instancias canónicas que la formulación bent NO cubre; por eso es
   finder propio y no un filtro del motor.
6. Options: canónico inmediatamente ANTES de Bent Quad (scores p.ej.
   230/250), ambos antes de ALS-XZ (5700); enabled=true ambos.
7. Solapamiento: es esperado y deseado que el solver muestre el canónico
   cuando ambos existen (el orden decide) y que all-steps liste ambos —
   precedente Skyscraper/Turbot. No filtres uno contra el otro.

FIXTURES Y CUSTODIA:
8. wxyz-wing.txt nuevo (código canónico): ≥2 positivos de CADA tipo (1 y
   2) + ≥2 negativos, uno obligatorio: un Bent Quad que NO es canónico —
   el caso del screenshot del 1.1 (r1c7,r5c7,r6c7,r2c8, Z=5) sirve tal
   cual. Cosechá del corpus; si algún tipo no aparece natural,
   construilo a mano y validalo con el harness.
9. Regla de custodia (sumala a Reglas de CLAUDE.md y a la plantilla):
   toda técnica nueva queda custodiada por ≥1 puzzle del corpus de
   snapshots cuyo solve path la contenga. Retroactivo: sumá 2-3 puzzles
   te2 cuyos paths default contengan Bent Quad y WXYZ canónico; regenerá
   snapshots con la tarea documentada y reportá el diff en el log.

ROADMAP Y QoL:
10. CLAUDE.md: renumerá — el nuevo 1.3 es "Bent subsets n=5..9
    (VWXYZ-Wing .. RSTUVWXYZ-Wing, entradas separadas con scores
    escalonados; defaults: 5 on, 6-9 off, estilo fish)" y los milestones
    siguientes se corren uno. Tick 1.2 al cerrar, PRÓXIMO al nuevo 1.3.
11. Lanzador sin terminal: tarea `gradlew run` + hodoku.bat en la raíz
    (doble clic → builda si hace falta y abre la GUI), documentado en
    docs/build.md para el dueño.

Métricas para el log: en corpus y te2-100, pasos canónicos por tipo vs
Bent Quads, y cuántos Bent Quads son también canónicos. Verificación GUI
de 1 canónico positivo. Revalidación soundness de todo lo cosechado.

Criterio de aceptación: suite verde local y en CI antes de tocar
snapshots (renombre + refactor) y después de la expansión de custodia
(con diff reportado); los dos archivos de fixtures corriendo; GUI
verificada; hodoku.bat andando. Al cerrar: commits lógicos (renombre /
refactor / técnica nueva / custodia+docs separados), push, link del run,
tick 1.2 + PRÓXIMO en 1.3, log técnico + resumen llano.
```
