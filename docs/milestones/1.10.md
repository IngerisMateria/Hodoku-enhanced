# Milestone 1.10 — Prompt original (archivado verbatim)

> Regla a) del CLAUDE.md: al arrancar cada milestone, su spec/prompt completo se archiva
> verbatim en `docs/milestones/NN.md`.

> Nota de encuadre: el 1.10 estaba planificado como "3D Medusa + GEM" en el roadmap y luego
> como "filtro de all-steps por celdas del patrón" en `docs/backlog-detalle_1.md`. El dueño
> lo REDEFINIÓ (2026-07-22) a un milestone de reestructuración de UI: toolbars configurables,
> estándar visual de tabs y fixes de UI. La find-pattern update se difiere a **P-010**.

```
Leé CLAUDE.md. Arrancamos el milestone 1.10, REDEFINIDO por el dueño (la find-pattern update se difiere a P-010): toolbars configurables + estándar visual de tabs + fixes de UI. CERO álgebra, CERO funciones nuevas, CERO cambios de solve paths. Todo reestructuración de UI. Archivá este prompt verbatim en docs/milestones/1.10.md (regla a).
TAREA A — Actualizar docs/pulido.md (el dueño ya lo reescribió y lo tenés en el repo; si no, te pasa backlog-detalle.md). Agregá P-010 con el detalle de abajo, y ajustá P-009 Parte 1/2 a "en curso → este milestone".
P-010 — Find-pattern update (filtro de all-steps por celdas): DIFERIDA (candidata Fable). Filtro en vivo POST-análisis: el usuario ilumina celdas y la lista de steps del panel de all-steps se filtra a las técnicas cuyo PATRÓN (no las eliminaciones) incluye celdas iluminadas; cero recomputo. Dos diseños ALTERNATIVOS (NO son fases, son modelos de interacción distintos — el dueño fue explícito):

* Alternativa A — switch global de 3 estados (apagado / restringido / abierto) en la toolbar superior; Ctrl+click ilumina; restringido = técnicas contenidas en la selección, abierto = técnicas que tocan alguna. Simple; no distingue celda por celda.
* Alternativa B — 2 layers de selección sobre el tablero: la selección amarilla existente + un segundo Ctrl+click sobre una celda ya seleccionada la marca como "abierta" (otro color) vs "restringida". Filtro: técnica pasa si contiene TODAS las restringidas y toca alguna abierta. Más expresivo (p.ej. x-wing restringido + aletas dudosas abiertas); riesgo: se mete con el modelo de selección del tablero. Botón on/off simple. Decisión de diseño pendiente (A vs B) para cuando se implemente. Ubicación del control: pendiente (ver P-009 — depende de toolbar configurable).

TAREA B — P-009 Parte 1/2, EXTENDIDA a las dos toolbars. Modelo: "conjunto de botones disponibles; el usuario elige cuáles se muestran y en qué orden". Esto cubre reordenar Y ocultar/reponer (2-en-1: un botón quitado vuelve a estar disponible para agregar). NO se agregan botones- función nuevos (eso sería P-005/P-009-parte-2); solo se gestiona lo que ya existe. B1. Toolbar SUPERIOR (filtros 1-9 + XY): array JToggleButton[10] en MainFrame. Reordenable + ocultable, preferencia persistida en Options. B2. Toolbar de ALL-STEPS (panel derecho): jToolBar1 en AllStepsPanel (configureButton + los *SortToggleButton). Verificado que es un JToolBar con .add() secuencial y toggles bien nombrados → estructura limpia, tratable igual que B1. Reordenable + ocultable, persistida. Si al implementar aparece un acoplamiento inesperado que lo vuelva riesgoso, FRENÁ y reportá antes de forzar — no rompas la toolbar que hoy anda. B3. UI de configuración: una pestaña/diálogo dentro de edit->preferences... como el resto de configuraciones, donde el usuario reordena y muestra/oculta los botones de AMBAS toolbars. Diseño claro y consistente con el estilo de las demás UI; sin functes nuevas. Persistencia y migración de Options.
TAREA C — Estándar visual de tabs (el dueño confirmó: TODAS las tabs). RIESGO CONOCIDO: las tabs son GroupLayout denso generado por NetBeans (no hay .form, así que editar el .java es seguro, pero GroupLayout es frágil: un cambio desalinea vecinos). Por eso: C1. Definí el estándar como CONSTANTES COMPARTIDAS en un helper nuevo (p.ej. sudoku.ui.UiMetrics): dimensión de viñeta (bullet) de categoría, ancho + anclaje de toggle estilo tab 'Steps' (anclado a la derecha, expande a la izquierda en proporción fija respecto de viñeta+texto — es el patrón que el dueño identificó como el bueno), ancho de input numérico. Valores nombrados, no mágicos. C2. Aplicá el estándar TAB POR TAB, corriendo la suite (gradlew test) entre cada tab, para detectar desalineaciones al instante y no al final. Orden sugerido por severidad reportada: General, Solver, All-steps (ConfigStepPanel es 'Steps'; ojo no confundir), Level/Font, Colors, y de paso Progress/Training/Colorku para uniformar aunque ya estén aceptables. C3. Fixes puntuales del dueño (dentro del estándar): - General: toggles de 'language' y 'look and feel' son interminables; viñetas demasiado extendidas. Acotar. - Level/Font: viñetas muy grandes; los botones de color de 'difficulty levels' tienen cada uno tamaño distinto → unificar a tamaño PEQUEÑO y consistente (el dueño adjuntó foto de referencia). Los inputs numéricos de 'factor' y score ya tienen buen tamaño: usalos como referencia del estándar de input. - Solver: todos los toggles enormes. - All-steps: toggles enormes e inconsistentes; viñetas muy grandes; la viñeta de ALS es ligeramente más corta que las otras → igualar. - Steps: viñetas e inputs muy largos; los toggles ya están bien (son la REFERENCIA del estándar de toggle). Acortar viñetas/inputs al estándar sin tocar el buen comportamiento del toggle. - Colors: viñetas de tamaño raro, con un hueco en el medio (no ocupan el espacio) → que ocupen parejo. - Progress / Training / Colorku: aceptables; solo alinear al estándar si es trivial, sin forzar.
TAREA D — Mínimo de popups. Hoy los diálogos se pueden achicar sin límite y la UI interna (mínimo fijo) queda cortada. Decisión del dueño: LIMITAR el mínimo del popup al mínimo de su contenido (no "más responsivo" — en pantalla chica igual no se vería). Aplicá setMinimumSize derivado del layout a los diálogos afectados; helper compartido si aplica a varios. Documentá cuáles cubriste.
VERIFICACIÓN GUI (capturas revisadas + contame): (a) reordenar y ocultar un botón en la toolbar superior, persistido tras reabrir; (b) lo mismo en la toolbar de all-steps; (c) 2-3 tabs mostrando el estándar aplicado (viñeta/toggle/input consistentes) — incluí Steps como referencia y una de las que estaban feas (Solver o All-steps); (d) un popup que ya no se achica más allá de su contenido usable. Matriz de config UX del 1.8 sigue verde.
Criterio de aceptación: suite verde local y en CI; las 4 verificaciones GUI; pulido.md con P-010 y P-009 actualizada; CLAUDE.md roadmap al día; bump ENHANCED_VERSION a "1.10"; cero álgebra, cero funciones nuevas, cero cambios de solve paths, TablingSolver y generador intactos. Al cerrar: commits lógicos (docs / toolbar-config / estándar-visual / popups separados), push, link del run, tick 1.10 + PRÓXIMO en el siguiente, log técnico + resumen llano.
```
