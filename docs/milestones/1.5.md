# Milestone 1.5 — Prompt original (archivado verbatim)

> Regla a) del CLAUDE.md: al arrancar cada milestone, su spec/prompt completo se archiva
> verbatim en `docs/milestones/NN.md`.

```
Leé CLAUDE.md y el paquete src/solver/modern/registry/ (el registro del
1.4 es tu materia prima). Arrancamos el milestone 1.5: Aside v2 +
buscador en config + split Kraken T1/T2. Archivá este prompt verbatim en
docs/milestones/1.5.md (regla a).

ASIDE V2 (ConfigSolverPanel — el panel que hoy solo tiene score y
dificultad):
1. Al seleccionar una técnica, el aside muestra además: descripción (del
   registro); "otros nombres" (aliases); selector de nombre preferido,
   usando la persistencia ENUM=alias del 1.4 — se refleja en las listas
   de config y en los hints modernos; los hints legacy quedan como están
   (limitación ya anotada en el log del 1.4).
2. Opciones: las que tienen a la técnica seleccionada como DUEÑA van
   editables in situ (sincronizadas con su pestaña clásica). Si el
   alcance excede a la técnica (multi-dueño, caso allowDualsAndSiamese),
   mostrá etiqueta de alcance "afecta también a: X, Y". El caso
   grisado + click-navega-al-dueño (afectada-pero-no-dueña) queda
   implementado estructuralmente; hoy no debería tener instancias — si
   aparece una, aplicalo y anotala.
3. Regla vigente: el aside es espejo; toda opción conserva su pestaña
   clásica canónica.

BUSCADOR:
4. Campo de filtro sobre la lista de técnicas del solver, matcheando
   nombre + aliases + descripción. Si el componente resulta reutilizable
   barato, aplicalo también a la lista de Find-All-Steps; si no, solo
   solver y anotá por qué.

SPLIT KRAKEN T1/T2 (P-002):
5. Des-colapsá el mapeo (SolutionType.getStepConfigType, ~línea 408):
   KRAKEN_FISH_TYPE_1 y TYPE_2 pasan a tener StepConfig propio (defaults
   ambas off como hoy; score/orden derivados del actual — documentá los
   elegidos), honrados por solver, find-all-steps y training. Decidí qué
   pasa con la entrada genérica KRAKEN_FISH (retirarla de la lista o
   dejarla no-configurable) sin romper persistencia, y documentalo.
6. Migración de hcfg viejos: la entrada única se migra a las dos nuevas
   preservando enabled/score. Test de roundtrip.
7. Registro: filas para TYPE_1 y TYPE_2 (el test de completitud lo va a
   exigir solo) y actualizá la ficha genérica de KRAKEN_FISH, que hoy
   dice "the split is P-002, milestone 1.5".
8. Test dirigido: cosechá UN estado de te3 que contenga kraken (con
   kraken habilitado en config de test), commitealo como fixture, y
   asertá que con TYPE_2 off el all-steps devuelve solo TYPE_1 ahí.
   Snapshots: kraken sigue off por default → cero cambios esperados,
   verificalo.
9. pulido.md: P-002 → cerrada/ejecutada con fecha y nota de cómo quedó.

VERIFICACIÓN GUI (capturas, revisalas y contame):
10. (a) aside de ALS-XZ con descripción + opciones ALS editables;
    (b) aside de Skyscraper o 2-String Kite mostrando la etiqueta de
    alcance de allowDualsAndSiamese; (c) la lista del solver con Kraken
    Type 1 y Type 2 separados; (d) el buscador encontrando Sue de Coq al
    tipear "disjoint".

Criterio de aceptación: suite verde local y en CI (test kraken nuevo +
custodia del registro exigiendo las filas nuevas); las 4 capturas
verificadas; migración de hcfg testeada; docs al día. Al cerrar: commits
lógicos (aside / buscador / kraken separados), push, link del run, tick
1.5 + PRÓXIMO en 1.6, log técnico + resumen llano.
```
